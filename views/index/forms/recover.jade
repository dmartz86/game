form.form.form-horizontal.col-md-12(name='recoverForm', ng-show='isEnabledView("recover")')
  p Recovery action will send a message to your email inbox with instructions to recover password.
  .form-group
    span.col-md-3.control-label Email
    .col-md-9
      input.form-control(type='email', ng-model='recover.email'
        required='required', placeholder='Email to recover')
  .form-group
    .col-md-9.col-md-offset-3
      a.btn.btn-primary.col-md-12(ng-click='login()',
        ng-disabled='recoverForm.$error.email || !recover.email') Get back my account
    .col-md-9.col-md-offset-3
      .col-md-6
        a(ng-click='enableView("login")') Show login
