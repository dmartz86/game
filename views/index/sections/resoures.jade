.well(ng-if='model')
  h3(ng-bind='"List for " + model')
  .row
    div(ng-repeat='f in feed')
      pre.col-md-10
        code(class="language-json") {{f}}
      a.col-md-1.btn.btn-primary
        i.glyphicon.glyphicon-pencil
