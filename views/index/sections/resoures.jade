div(ng-if='model')
  h3(ng-bind='$parent.filter')
  .row
    div(ng-repeat='f in $parent.feed | filter:search')
      pre.col-md-10
        code(class="language-js") {{f}}
      a.col-md-1.btn.btn-primary
        i.glyphicon.glyphicon-pencil
